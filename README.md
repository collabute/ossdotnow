# oss.now Web

TanStack Start frontend for oss.now.

## Development

```sh
bun install
bun run dev
```

The frontend listens on `http://localhost:3000` and expects the API at `VITE_API_BASE_URL`.

## Docs

- [Environment](docs/environment.md)
